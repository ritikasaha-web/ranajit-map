# change the api calls at sitePhotos.tsx and siteUploadPhotos.tsx to the server url after pushing.

# do git stash before git pull on server otherwise it wont work because it will recieve a change manually once you change the url manually.
